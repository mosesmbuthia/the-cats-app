const form = document.getElementById("form");
form.addEventListener("submit" , async (e) => {
    e.preventDefault();
    const formVal = document.getElementById("form");
    const factVal = document.getElementById("factInput");
    const displaVal = document.getElementById("display");
    console.log(displaVal);
});